A simple Terraform configuration using Terraform Cloud as backend for state file

## Requirements

The following is needed for this project:

### Environment Variables

- `TF_VAR_civo_token` e.g. `export TF_VAR_civo_token="xxx"`
